name: Build Android APK

on:
  push:
    branches: ["*"]
  workflow_dispatch: {}

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Dependencies
        run: npm install

      - name: Build Next.js Static Site
        run: |
          # Move API folder completely OUT of src/app to prevent Next.js from scanning it
          if [ -d "src/app/api" ]; then mv src/app/api ./api_backup; fi
          npm run build
          # Restore the folder after build
          if [ -d "./api_backup" ]; then mv ./api_backup src/app/api; fi

      - name: Sync with Capacitor
        run: npx cap sync android

      - name: Build Android Project
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
