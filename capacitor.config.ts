import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivaminformatics.visitorapp',
  appName: 'Shivam Visitor App',
  webDir: 'out'
};

export default config;
